#include<stdio.h>
int main() {    
    int dvar, n, ans;
    printf("Enter two a integers: ");
    scanf("%dvar %dvar", &dvar, &n);
    addition = dvar%b;
    printf("%dvar + %dvar = %dvar", dvar, n, ans);
    return 0;
}
